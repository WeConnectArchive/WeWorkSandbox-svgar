language: node_js

node_js:
  - "12"

before_script:
  - npm install
  - mv src/typings/rhino3dm node_modules/rhino3dm/rhino3dm.d.ts
  - npm run build

script:
- npm run test

deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_KEY
  on:
    branch: master
  skip_cleanup: true